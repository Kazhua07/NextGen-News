<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Register App</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap');
        
        body {
            background-color: #f8f9fa;
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
        }

        .form-card {
            background-color: #ffffff;
            border-radius: 12px;
            padding: 40px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            margin: 0 auto;
            transform: translateY(0);
            transition: all 0.4s ease;
        }

        .form-card:hover {
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.15);
            transform: translateY(-5px);
        }

        .form-card h2 {
            margin-bottom: 25px;
            font-weight: 600;
            color: #343a40;
            text-align: center;
            position: relative;
        }

        .form-card h2::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 50px;
            height: 3px;
            background: #28a745;
            border-radius: 3px;
        }

        .form-control {
            border-radius: 8px;
            padding: 12px 15px;
            border: 1px solid #e1e5ee;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            border-color: #28a745;
            box-shadow: 0 0 0 0.2rem rgba(40, 167, 69, 0.25);
        }

        .btn-success {
            background-color: #28a745;
            border: none;
            border-radius: 8px;
            padding: 12px;
            font-weight: 500;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
        }

        .btn-success:hover {
            background-color: #218838;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(40, 167, 69, 0.3);
        }

        .mssg {
            margin-top: 15px;
            opacity: 0;
            height: 0;
            overflow: hidden;
            transition: all 0.3s ease;
        }

        .mssg.show {
            opacity: 1;
            height: auto;
            padding: 10px;
        }

        .bg-danger {
            color: #ffffff;
            background-color: #8076e9;
            border-radius: 8px;
        }

        .footer-text {
            margin-top: 20px;
            text-align: center;
            color: #6c757d;
        }

        .footer-text a {
            color: #28a745;
            font-weight: 500;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .footer-text a:hover {
            color: #218838;
            text-decoration: underline;
        }

        /* Input field animations */
        .form-group {
            position: relative;
            margin-bottom: 25px;
        }

        .form-group input {
            padding-left: 40px;
        }

        .form-group i {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #6c757d;
            transition: all 0.3s ease;
        }

        .form-control:focus + i {
            color: #28a745;
        }

        /* Success animation */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .animate-fadeIn {
            animation: fadeIn 0.5s ease forwards;
        }
    </style>
    <script type="text/javascript">
        $(document).ready(() => {
            // Add icons to input fields
            $('<i class="fas fa-envelope"></i>').insertBefore('input[name="email"]');
            $('<i class="fas fa-user"></i>').insertBefore('input[name="username"]');
            $('<i class="fas fa-lock"></i>').insertBefore('input[name="password"]');
            $('<i class="fas fa-lock"></i>').insertBefore('input[name="passwordConf"]');

            // Load Font Awesome
            $('head').append('<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">');

            $("#form1").submit((event) => {
                event.preventDefault();

                // Add loading animation
                $('button[type="submit"]').html('<span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span> Registering...');

                $.ajax({
                    type: 'POST',
                    url: '/',
                    data: $('#form1').serialize(),
                    dataType: "json",
                    success: (response) => {
                        $('#form1')[0].reset();
                        $('button[type="submit"]').html('Register');
                        
                        $('#check').text(response.Success)
                            .parent()
                            .addClass('show animate__animated animate__fadeIn');
                        
                        setTimeout(() => {
                            $('#check').text("").parent()
                                .removeClass('show animate__animated animate__fadeIn')
                                .addClass('animate__animated animate__fadeOut');
                        }, 3000);

                        if (response.Success == "You are registered, You can login now.") {
                            setTimeout(() => {
                                document.getElementById("aa").click();
                            }, 1000);
                        }
                    },
                    error: () => {
                        $('button[type="submit"]').html('Register');
                        $('#check').text("An error occurred. Please try again.")
                            .parent()
                            .addClass('show animate__animated animate__shakeX');
                    }
                });
            });

            // Animate form on load
            $('.form-card').addClass('animate__animated animate__fadeInUp');
        });
    </script>
</head>

<body>

    <div class="container">
        <div class="col-md-6 col-lg-4 mx-auto">
            <div class="form-card animate__animated">
                <h2>Register Now</h2>
                <form id="form1" method="post">
                    <div class="form-group">
                        <input type="text" name="email" placeholder="E-mail" required="" pattern="[-!#$%&'*+\/0-9=?ก-๙A-Z^_a-z`{|}~](\.?[-!#$%&'*+\/0-9=?ก-๙A-Z^_a-z`{|}~])*@[ก-๙a-zA-Z0-9](-*\.?[ก-๙a-zA-Z0-9])*\.[ก-๙a-zA-Z](-?[ก-๙a-zA-Z0-9])+$" class="form-control">
                    </div>
                    <div class="form-group">
                        <input type="text" name="username" placeholder="Username" required="" class="form-control">
                    </div>
                    <div class="form-group">
                        <input type="password" name="password" placeholder="Password" required="" class="form-control">
                    </div>
                    <div class="form-group">
                        <input type="password" name="passwordConf" placeholder="Confirm Password" required="" class="form-control">
                    </div>
                    <button type="submit" class="btn btn-success btn-block">
                        Register
                    </button>
                </form>

                <div class="mssg bg-danger">
                    <span id="check"></span>
                </div>

                <div class="footer-text">
                    <span>Already Registered? <a href="/login">Login</a></span>
                </div>
                <div id="LangTable">
                    <a href="/login" id="aa" style="display:none;"></a>
                </div>
            </div>
        </div>
    </div>

</body>

</html>
